{
    "name": "fleet-tower/essentials",
    "description": "Just better defaults for your Laravel projects.",
    "keywords": ["php", "laravel", "package", "essentials", "laravel-essentials"],
    "license": "MIT",
    "authors": [
        {
            "name": "Nuno Maduro",
            "email": "enunomaduro@gmail.com"
        }
    ],
    "require": {
        "php": "^8.4",
        "laravel/framework": "^12.32.5"
    },
    "require-dev": {
        "laravel/pint": "^1.25.0",
        "orchestra/testbench": "^10.6.0",
        "pestphp/pest": "^3.8.4",
        "pestphp/pest-plugin-type-coverage": "^3.6.1",
        "phpstan/phpstan": "^2.1.27",
        "rector/rector": "^2.1.7",
        "roave/security-advisories": "dev-latest",
        "symfony/var-dumper": "^7.3.3"
    },
    "autoload": {
        "psr-4": {
            "FleetTower\\Essentials\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Tests\\": "tests/"
        }
    },
    "minimum-stability": "stable",
    "prefer-stable": true,
    "config": {
        "optimize-autoloader": true,
        "sort-packages": true,
        "preferred-install": "dist",
        "allow-plugins": {
            "pestphp/pest-plugin": true
        }
    },
    "scripts": {
        "refactor": "rector",
        "lint": "pint",
        "test:refactor": "rector --dry-run",
        "test:lint": "pint --test",
        "test:types": "phpstan analyse --memory-limit=2G --ansi",
        "test:unit": "pest --colors=always --coverage --parallel",
        "test": [
            "@test:refactor",
            "@test:lint",
            "@test:types",
            "@test:unit"
        ]
    },
    "extra": {
        "laravel": {
            "providers": [
                "FleetTower\\Essentials\\EssentialsServiceProvider"
            ]
        }
    }
}
